#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

if [[ "$WATCH" == "" ]]; then
  npx postcss src/main.css -o ./src/phlex_chatbot.css
else
  npx postcss src/main.css --verbose -o ./src/phlex_chatbot.css --watch
fi
