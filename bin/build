#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

if [[ "$WATCH" == "" ]]; then
  npx tailwindcss build -o ./src/hedgeye_chatbot.css
else
  npx tailwindcss build -o ./src/hedgeye_chatbot.css --watch
fi
